apiVersion: v1
kind: Service
metadata:
  name: posapi-svc
  annotations:
    metallb.universe.tf/address-pool: test
    metallb.universe.tf/loadBalancerIPs: 10.90.0.113
spec:
  selector:
    app: posapi
  ports:
  - protocol: TCP
    port: 7080
    targetPort: 7080
  type: LoadBalancer

