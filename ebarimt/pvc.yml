apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: posapi-pvc
spec:
  accessModes:
  - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 3Gi
  storageClassName: dellcsi-powerstore-nfs-test

