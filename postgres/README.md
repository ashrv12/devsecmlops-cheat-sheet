> [!IMPORTANT]
> Docker automatically detects and deploys the postgres instance with the .env file if it is within
> the same directory as the compose file. SO REMEMBER TO CREATE THE ENVS!
>
> ${POSTGRES_USER} ${POSTGRES_PASSWORD} ${POSTGRES_DB}
