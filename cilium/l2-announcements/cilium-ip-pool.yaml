apiVersion: "cilium.io/v2"
kind: CiliumLoadBalancerIPPool
metadata:
    name: homelab-ip-pool
    namespace: kube-system
spec:
    blocks:
        - cidr: "192.168.1.240/28" # Change this range to match your network topology
